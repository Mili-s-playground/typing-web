{
  "name": "parcel-deployment",
  "version": "1.0.0",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "clean": "rm -rf docs",
    "build": "npm run clean && parcel build --dist-dir docs --public-url /typing-web/"
  },
  "devDependencies": {
    "parcel": "^2.13.3"
  }
}
